{
  "name": "rubics-cube",
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf ./dist && tsc && copyfiles ./package.json ./dist",
    "publish": "cd ./dist && npm publish"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^4.4.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  }
}